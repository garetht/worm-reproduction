from pathlib import Path

rag_emails_csv_dir = '../database/all_emails.csv' #  a path to the CSV file with the application emails

# root of the repository source
current_path = Path(__file__).resolve().parent.parent
